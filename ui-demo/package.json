{
  "name": "hutool-admin-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "gulp dev",
    "build": "rimraf dist && gulp build",
    "upload": "rimraf dist && gulp build && gulp do-upload && rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "del": "^6.1.1",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "gulp-sftp-up4": "^0.1.8",
    "path": "^0.12.7",
    "sass": "^1.52.3"
  }
}
